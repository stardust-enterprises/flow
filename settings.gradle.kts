pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

plugins {
//    id("enterprises.stardust.flow.root") version "0.1.8"
    id("enterprises.stardust.flow") version "0.2.0"
}